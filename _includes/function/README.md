Files with preceding `--` are the uncompressed files.
```
capitalize.liquid ->
    @param string (string)

remove-extension.liquid ->
    @param path (fill path)

title.liquid ->
    @param title (page title)
    @param name (file name)
```